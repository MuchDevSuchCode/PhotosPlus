<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Galileo.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- App-level so the WinUI markup compiler is happy (Window.Resources styles crash it). -->
            <Style x:Key="GlyphButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Width" Value="44" />
                <Setter Property="Height" Value="44" />
                <Setter Property="CornerRadius" Value="22" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <SolidColorBrush x:Key="PillBrush" Color="#E6262626" />

            <!-- Section header used by the Settings panel and the video editor (app-level so both see it). -->
            <Style x:Key="SettingSectionHeader" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Opacity" Value="0.6" />
                <Setter Property="CharacterSpacing" Value="80" />
                <Setter Property="Margin" Value="2,0,0,0" />
            </Style>

            <!-- Clickable Details-view column header -->
            <Style x:Key="ColumnHeaderButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="2,0" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            </Style>

            <!-- Explorer: icon-view tile (cell size set via ItemsWrapGrid.ItemWidth/Height) -->
            <DataTemplate x:Key="ExplorerIconTemplate">
                <Grid Padding="6" Opacity="{Binding ItemOpacity}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image Source="{Binding Icon}" Stretch="Uniform"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4" />
                    <StackPanel Grid.Row="1">
                        <TextBlock Text="{Binding DisplayName}" FontSize="12" Margin="0,2,0,0"
                                   TextAlignment="Center" TextWrapping="Wrap" MaxLines="2"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTipService.ToolTip="{Binding Name}" />
                        <!-- Drive capacity bar + "X free of Y" (drives only) -->
                        <ProgressBar Maximum="100" Value="{Binding UsedPercent}" Height="6" MinHeight="6" Margin="6,4,6,0"
                                     Visibility="{Binding CapacityVisibility}" />
                        <TextBlock Text="{Binding CapacityText}" FontSize="10" Opacity="0.7" Margin="0,2,0,0"
                                   TextAlignment="Center" TextTrimming="CharacterEllipsis"
                                   Visibility="{Binding CapacityVisibility}" />
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <!-- Explorer: details row -->
            <DataTemplate x:Key="ExplorerDetailsTemplate">
                <Grid MinHeight="30" Opacity="{Binding ItemOpacity}">
                    <Grid.ColumnDefinitions>
                        <!-- NOTE: these column widths must stay in sync with the details header row in MainWindow.xaml -->
                        <ColumnDefinition Width="*" MinWidth="180" />
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="90" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                        <Image Source="{Binding Icon}" Width="20" Height="20" />
                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                   ToolTipService.ToolTip="{Binding Name}" />
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding ModifiedText}" VerticalAlignment="Center" Opacity="0.8" FontSize="13" />
                    <TextBlock Grid.Column="2" Text="{Binding TypeName}" VerticalAlignment="Center" Opacity="0.8" FontSize="13" />
                    <TextBlock Grid.Column="3" Text="{Binding SizeText}" VerticalAlignment="Center" Opacity="0.8" FontSize="13" TextAlignment="Right" />
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
